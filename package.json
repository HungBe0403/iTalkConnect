{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Chat application with React frontend and Node.js backend",
  "scripts": {
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:server\"",
    "dev:web": "cd web && npm run dev",
    "dev:server": "cd server && npm run dev",
    "install:all": "concurrently \"cd web && npm install\" \"cd server && npm install\"",
    "build": "cd web && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}